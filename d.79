void main (){
  (int,int)? records = (4,5);
  print(records);
  records = null;
  print(records);
}

output:
(4, 5)
null

//we can also use the initialise null to the records.
// records are real values. you can store them in variables, pass them to and from functions and store them in lists.
